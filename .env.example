# Public site URL (used for canonical, sitemap, og:url)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# New Postgres database (Drizzle)
DATABASE_URL=postgres://sportlive:sportlive@localhost:5433/sportlive

# Legacy DLE MariaDB (read-only adapter for migration; production uses VPS tunnel)
DLE_DATABASE_URL=mysql://sportlive:bek_orzi@127.0.0.1:3311/sportlive

# Auth (better-auth)
BETTER_AUTH_SECRET=replace-me-with-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:3000
